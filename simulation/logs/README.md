# Logs

This directory stores local simulation and ROS-side logs for the WSL2 robot workspace.

- `logs/ros/` is used by local ROS tools when `source_sim.sh` is active.
- `logs/sim/` is reserved for simulation-side logs and future pipeline output.
